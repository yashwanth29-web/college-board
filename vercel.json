{
  "version": 2,
  "buildCommand": "npm install && npm run build -- --configuration=production",
  "outputDirectory": "dist/student-portal/browser",
  "installCommand": "npm install",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
